# Optional — leave unset to use the bundled local food database.
# When set, the app reads the `foods` table populated by the ingestion
# pipeline (see ingest/). Use the ANON key here, never the service role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
